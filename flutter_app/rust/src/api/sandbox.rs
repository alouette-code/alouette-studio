/// Get sandbox configs
pub async fn load_sandbox_configs() -> Result<Vec<String>, String> {
    Ok(vec![])
}

/// Save sandbox config
pub async fn save_sandbox_config(_json: String) -> Result<(), String> {
    Ok(())
}
